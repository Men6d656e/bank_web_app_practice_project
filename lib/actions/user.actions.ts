'use server';

export const signIn = () => {
    try {
        // 
    } catch (error) {
        console.log(error)
    }
}


export const signUp = (userData:SignUpParams) => {
    try {
        // create a user account
    } catch (error) {
        console.log(error)
    }
}